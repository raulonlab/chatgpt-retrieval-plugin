# Source: https://github.com/render-examples/chatgpt-retrieval-plugin/blob/main/render.yaml
# Other files:
  # https://github.com/render-examples/chatgpt-retrieval-plugin/blob/main/Dockerfile
  # https://github.com/render-examples/chatgpt-retrieval-plugin/blob/main/Dockerfile.weaviate

services:
  # FastAPI Server
  - type: web
    runtime: docker
    name: retrieval-plugin-server 
    # region: oregon
    # plan: starter
    dockerfilePath: ./Dockerfile
    envVars: 
      - key: DATASTORE
        value: chatnerds
      - key: BEARER_TOKEN
        generateValue: true 
      # will be promtped to add this in the UI on deployment
      - key: OPENAI_API_KEY
        sync: false
      - key: ACTIVE_NERD
        sync: false
      - key: NERDS_DIRECTORY_PATH
        value: /data/nerds
    disk:
      name: data-disk
      mountPath: /data/
      sizeGB: 1

  # # Weaviate vector database
  # - type: pserv
  #   name: weaviate
  #   region: oregon
  #   plan: starter
  #   env: docker
  #   dockerfilePath: ./Dockerfile.weaviate
  #   autoDeploy: false
  #   disk:
  #     name: weaviate-data
  #     mountPath: /var/lib/weaviate
  #     sizeGB: 4
  #   envVars:
  #     - key: QUERY_DEFAULTS_LIMIT
  #       value: 25
  #     - key: AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED
  #       value: true
  #     - key: PERSISTENCE_DATA_PATH
  #       value: /var/lib/weaviate
  #     - key: DEFAULT_VECTORIZER_MODULE
  #       value: none
  #     - key: CLUSTER_HOSTNAME
  #       value: node1
